# ==============================================================================
# MODELO DE VARIÁVEIS DE AMBIENTE - VIVALDI BANK
# ==============================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo e renomeie para ".env.dev" ou ".env.prod"
# 2. Preencha os valores conforme o ambiente desejado.
# 3. NUNCA comite os arquivos .env reais com senhas!
# ==============================================================================

# Perfil do Spring (dev = LocalStack / prod = AWS Real)
SPRING_PROFILES_ACTIVE=dev

# --- Banco de Dados (PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vivaldi_bank
DB_USER=postgres
DB_PASSWORD=postgres

# --- Credenciais AWS ---
# Em DEV: use 'test'
# Em PROD: RECOMENDADO: Não preencha aqui. Configure via AWS CLI ('aws configure')
# ou variáveis de ambiente do sistema para maior segurança.
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_REGION=us-east-1

# --- Configuração LocalStack (SOMENTE PARA DEV) ---
# Em PROD: Remova ou comente esta linha para usar a AWS Real
SPRING_CLOUD_AWS_ENDPOINT=http://localhost:4566

# --- Segurança JWT ---
JWT_SECRET=segredo-padrao-desenvolvimento

# --- Monitoramento (Grafana) ---
GF_SECURITY_ADMIN_PASSWORD=admin
